{
  "name": "cortex",
  "version": "0.1.0",
  "type": "module",
  "description": "Knowledge infrastructure — graph builder, observer, vault tools",
  "bin": {
    "cortex-graph": "./src/graph/cli.js"
  },
  "scripts": {
    "test": "node --test test/graph/*.test.js test/observer/*.test.js test/integration/*.test.js",
    "test:graph": "node --test test/graph/*.test.js",
    "test:observer": "node --test test/observer/*.test.js",
    "start:explorer": "node explorer/server.js"
  }
}
